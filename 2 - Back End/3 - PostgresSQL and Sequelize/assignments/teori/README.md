# Soal Teori - PostgreSQL and Sequelize

1. **Apa itu ORM (Object Relational Mapping)?**
2. **Kenapa menggunakan Sequelize dibanding raw query SQL?**
3. **Apa itu migration dalam Sequelize?**
4. **Apa itu seeder dalam Sequelize?**
5. **Apa itu model dalam Sequelize?**
6. **Apa fungsi perintah sequelize-cli seperti `sequelize db:migrate`?**
7. **Apa itu connection pooling dalam koneksi database?**
8. **Sebutkan beberapa tipe data umum di PostgreSQL.**
9. **Apa perbedaan antara `sync()` dan migration di Sequelize?**
10. **Apa itu environment configuration (config.json/.env) dalam project Sequelize?**
