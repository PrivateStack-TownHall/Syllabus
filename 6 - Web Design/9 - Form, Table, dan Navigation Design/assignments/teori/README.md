# Soal Teori - Form, Table, dan Navigation Design

1. **Apa fungsi tag `<form>` dalam HTML?**
2. **Sebutkan beberapa jenis input yang umum dipakai dalam form HTML.**
3. **Apa fungsi tag `<label>` dan kenapa penting dihubungkan dengan input menggunakan atribut `for`?**
4. **Apa fungsi tag `<table>`, `<tr>`, `<td>`, dan `<th>` dalam HTML?**
5. **Kapan sebaiknya menggunakan tabel HTML, dan kapan sebaiknya dihindari untuk keperluan layout?**
6. **Apa fungsi atribut `required` pada elemen input form?**
7. **Apa perbedaan input `type="radio"` dan `type="checkbox"`?**
8. **Bagaimana struktur dasar navigasi (navigation bar) yang umum dipakai di HTML?**
9. **Apa fungsi atribut `placeholder` pada input form?**
10. **Kenapa penting mendesain form dan navigasi yang mudah digunakan (user-friendly), bukan hanya terlihat bagus?**
