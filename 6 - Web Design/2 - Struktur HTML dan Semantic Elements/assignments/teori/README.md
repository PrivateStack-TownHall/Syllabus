# Soal Teori - Struktur HTML dan Semantic Elements

1. **Apa yang dimaksud dengan semantic elements dalam HTML?**
2. **Apa perbedaan `<div>` dengan elemen semantic seperti `<section>` atau `<article>`?**
3. **Apa fungsi tag `<nav>` dalam sebuah halaman web?**
4. **Apa fungsi tag `<header>` dan `<footer>`?**
5. **Kenapa penggunaan semantic elements dianggap lebih baik untuk SEO dan aksesibilitas?**
6. **Apa fungsi tag `<main>` dalam sebuah halaman web?**
7. **Apa perbedaan heading `<h1>` sampai `<h6>`, dan kenapa urutannya penting?**
8. **Apa fungsi atribut `alt` pada tag `<img>`?**
9. **Apa itu elemen block-level dan inline dalam HTML? Berikan contohnya.**
10. **Kenapa struktur HTML yang rapi dan semantic memudahkan proses styling dengan CSS di tahap berikutnya?**
