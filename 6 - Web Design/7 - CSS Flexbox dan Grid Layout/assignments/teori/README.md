# Soal Teori - CSS Flexbox dan Grid Layout

1. **Apa itu Flexbox dan kapan biasanya digunakan?**
2. **Apa fungsi properti `justify-content` dalam Flexbox?**
3. **Apa fungsi properti `align-items` dalam Flexbox?**
4. **Apa itu CSS Grid dan apa perbedaannya dengan Flexbox?**
5. **Apa fungsi properti `grid-template-columns` dalam CSS Grid?**
6. **Apa itu satuan `fr` dalam CSS Grid?**
7. **Kapan sebaiknya memilih Grid dibanding Flexbox untuk sebuah layout?**
8. **Apa fungsi properti `flex-wrap` dalam Flexbox?**
9. **Apa fungsi properti `gap` yang bisa dipakai baik di Flexbox maupun Grid?**
10. **Bagaimana Flexbox dan Grid bisa dipakai bersamaan dalam satu halaman?**
