# Soal Teori - React Router DOM

1. **Apa itu SPA (Single Page Application)?**
2. **Apa perbedaan client-side routing dan server-side routing?**
3. **Apa fungsi React Router DOM dalam aplikasi React?**
4. **Apa itu komponen `<Route>` dan `<Routes>` dalam React Router?**
5. **Apa itu dynamic route params, misalnya `/users/:id`?**
6. **Apa fungsi komponen `<Link>` dibanding tag `<a>` biasa?**
7. **Apa itu nested route dalam React Router?**
8. **Bagaimana cara melakukan redirect secara programatis di React Router?**
9. **Apa fungsi `useParams()` dan `useSearchParams()`?**
10. **Kenapa penting menyiapkan halaman fallback (404 / Not Found) dalam React Router?**
